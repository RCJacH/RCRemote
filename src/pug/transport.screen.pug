mixin btn_screen(id)
  div(class="c-screen__button")
    .o-icon(id=id + "-button",)
      svg(viewBox="0 0 100 100")
        use(xlink:href="#o-icon-" + id)

.c-screen
  +btn_screen("backward")
  .c-screen__info
    #status
    #position
  +btn_screen("forward")
