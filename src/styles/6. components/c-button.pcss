.c-button {
  $self: &;
  display: flex;
  flex: 1 1 auto;
  justify-content: center;
  align-items: center;
  flex-direction: row;
  width: 100%;
  height: 100%;

  & button {
    cursor: pointer;
  }

  &.-base {
    background-color: transparent;
  }

  & .o-icon {
    z-index: 1;
    margin: auto;
    width: 40%;
    height: 40%;
  }
}
