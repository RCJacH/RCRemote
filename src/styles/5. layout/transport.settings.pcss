#transport-settings {
  display: flex;
  flex-wrap: wrap;
  flex-direction: row;
  flex: 2;

  @media screen and (orientation: landscape) {
    flex: 1.2;
    flex-wrap: unset;
  }

  & .c-button {
    flex: 1 1 auto;
    flex-direction: row;
    height: auto;
  }
}
